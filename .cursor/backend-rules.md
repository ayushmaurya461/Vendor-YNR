# Backend Rules — YNR Local

The **canonical** backend conventions and API contracts live in:

**`/docs/backend-rules.md`**

Open that file when working on `apps/api` or any Fastify/Mongo backend code.
